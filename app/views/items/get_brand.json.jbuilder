json.array! @fashion1 do |fashion|
  json.id fashion.id
  json.name fashion.name
end


json.array! @fashion2 do |fashion|
  json.id fashion.id
  json.name fashion.name
end

json.array! @fashion3 do |fashion|
  json.id fashion.id
  json.name fashion.name
end

json.array! @fashion4 do |fashion|
  json.id fashion.id
  json.name fashion.name
end

json.array! @fashion5 do |fashion|
  json.id fashion.id
  json.name fashion.name
end
